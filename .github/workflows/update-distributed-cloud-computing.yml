name: Update DCC README

on:
  push:
    paths:
      - 'distributed-cloud-computing/**'

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Generate project list
        run: |
          cd distributed-cloud-computing
          echo "" > list.md
          for d in */ ; do
            if [ "$d" != "./" ]; then
              echo "- 🔹 [${d%/}](./${d%/})" >> list.md
            fi
          done

      - name: Inject into README
        run: |
          awk '
          /<!--START_SECTION:projects-->/ {print; system("cat distributed-cloud-computing/list.md"); skip=1; next}
          /<!--END_SECTION:projects-->/ {skip=0}
          !skip
          ' distributed-cloud-computing/README.md > temp.md
          mv temp.md distributed-cloud-computing/README.md

      - name: Commit changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add .
          git commit -m "Auto-update DCC project list" || echo "No changes"
          git push
